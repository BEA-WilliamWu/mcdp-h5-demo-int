{
  "name": "hth-consulting-frontend-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Local BCO frontend server and backend proxy",
  "scripts": {
    "start": "node server.js",
    "test": "node --test server.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
