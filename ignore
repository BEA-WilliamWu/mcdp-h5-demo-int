用了两种仅本地生效的设置：
- .git/info/exclude：忽略尚未被 Git 跟踪的 .project。
- skip-worktree：让已跟踪的 .project 暂时不显示本地修改。只写 .gitignore 对这些文件无效。
Windows 用 PowerShell，先进入项目仓库，再执行：
cd "D:\你的路径\hth-application"

# 忽略新出现的 .project，仅本机生效
$excludePath = git rev-parse --git-path info/exclude
Add-Content -LiteralPath $excludePath -Value "`n.project" -Encoding ascii

# 跳过已跟踪 .project 的本地修改
git ls-files -- ".project" ":(glob)**/.project" |
    ForEach-Object {
        git update-index --skip-worktree -- $_
    }

# 检查结果
git status --short
.project 的修改应不再出现在待提交列表。文件内容保留，不需要 commit 或 push。每个本地仓库分别执行一次。
以后需要更新或提交这些已跟踪文件时，先恢复：
git ls-files -- ".project" ":(glob)**/.project" |
    ForEach-Object {
        git update-index --no-skip-worktree -- $_
    }
如果 pull 提示 .project 的本地改动冲突，也先恢复标记，再正常处理差异。